{
	"name": "greg",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"workspaces": ["packages/*"],
	"scripts": {
		"dev": "bun run --filter '*' dev",
		"dev:server": "bun run --filter @greg/server dev",
		"dev:frontend": "bun run --filter @greg/frontend dev",
		"build": "bun run --filter @greg/frontend build"
	},
	"devDependencies": {
		"bun-types": "^1.3.11",
		"sass-embedded": "^1.99.0"
	}
}
