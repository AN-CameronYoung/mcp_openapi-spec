"use client";

const SettingsPage = (): JSX.Element => {
  return (
    <div className="flex flex-col flex-1 items-center justify-center text-sm text-muted-foreground p-8">
      Use the Admin page to ingest APIs and docs.
    </div>
  );
};

export default SettingsPage;
